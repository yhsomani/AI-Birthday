# Failed isolated CLI council attempt

This run is invalid and excluded from synthesis.

The sandboxed Codex child processes could not initialize the user-level Codex state database. A requested escalated retry was denied because a separate authenticated CLI process would export repository-derived context outside the current authorized workspace session. The denial was not bypassed.

The valid, privacy-safer in-session Codex-only council is stored in ../20260718-native-codex-only-master-prompt/.
