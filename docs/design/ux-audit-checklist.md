# UX Audit Checklist

Last reviewed: 2026-06-28

This checklist tracks the RelateAI UI redesign without changing feature behavior. A screen is production-ready only when its purpose, owner feature, navigation entry, empty/loading/error states, accessibility, and tests are verified.

## Audit Principles

- Each feature has one primary home. Other screens may link to it, summarize it, or expose a contextual shortcut, but must not duplicate full ownership.
- Primary navigation is for repeated daily workflows. Secondary routes are allowed for setup, detail, audit, and recovery flows.
- Every screen must answer: what is the user trying to do, what state is the app in, what is the next safe action, and how do they recover?
- Visual redesign must preserve ViewModel behavior, route contracts, persistence contracts, dispatch eligibility, and permission gates.
- Screens must be readable at large font scale and narrow mobile widths before a task is closed.

## Screen Ownership Decisions

| Screen | Current route | Decision | Primary purpose | Merge or cleanup rule |
| --- | --- | --- | --- | --- |
| Onboarding | `onboarding` | Keep as entry/setup bridge | Explain product value and route users to setup. | Do not duplicate AI Doctor checks; show setup progress summary and link to AI Doctor. |
| Home | `home` | Keep as operational dashboard | Show next best action, setup progress, upcoming work, and critical stats. | Home owns summaries only. Full queues stay in Messages, full reports stay in Analytics, full setup stays in AI Doctor. |
| Contacts | `contacts` | Keep in bottom navigation | Search, sync, filter, sort, and open contact records. | Contact editing and relationship context stay in Contact Detail. |
| Contact Detail | `contacts/{contactId}` | Keep as contextual workspace | Relationship profile, preferences, events, memory/gift/style links, and history context. | Memory Vault, Gift Advisor, Style Coach, and Chat History should be launched contextually from here, not duplicated as primary nav. |
| Events | `events` | Keep in bottom navigation | Occasion management, manual events, conflicts, and verification. | Message review stays in Messages/Wish Preview; Events should not become a send queue. |
| Messages | `messages` | Keep in bottom navigation | Review, scheduled, blocked, failed, sent, and bulk recovery queues. | Messages owns queue state. Wish Preview owns one draft review/edit flow. |
| Wish Preview | `wish/{contactId}/{messageRef}` | Keep as detail task flow | Review/edit/regenerate/test/approve/reject one draft. | Do not expose full queue management here beyond next-review affordance. |
| Analytics | `analytics` | Keep in bottom navigation for now | Reporting, health distribution, trend summaries, CSV export. | Must not duplicate Home next actions or Activity History audit rows. If usage is low, consider moving to secondary More/Insights later. |
| Activity History | `activity-history` | Keep as secondary audit route | Filterable operational audit trail. | Analytics can link to it; do not duplicate raw audit tables in Analytics. |
| AI Doctor | `automation-setup` | Keep as setup/recovery center | Permissions, channels, AI, exact alarms, WorkManager, recovery checks, dry run. | Settings may link to it, but channel setup/diagnostics live here. |
| Settings | `settings` | Keep as secondary/global route | Credentials, global preferences, sign-out, links to setup/data tools. | Avoid duplicating AI Doctor diagnostics or Backup workflows. |
| Backup/Restore | `backup-restore` | Keep as secondary data route | Export, preview, restore, and backup status. | Settings/Home may link; backup logic stays here. |
| Memory Vault | `memory-vault/{contactId}` | Keep contextual | Contact memory notes. | No primary navigation until there is a cross-contact memory workflow. |
| Gift Advisor | `gift-advisor/{contactId}` | Keep contextual | Gift history and suggestions for a contact. | No primary navigation until there is a cross-contact gift planner. |
| Style Coach | `style-coach/{contactId}` | Keep contextual | Writing style analysis/profile for a contact. | Should feed personalization context; avoid duplicating Wish Preview editing. |
| Chat History | `chat-history/{contactId}` | Keep contextual or merge candidate | Contact-specific conversation/message history. | Candidate to merge into Contact Detail history tab if future UI adds tabs. Do not remove until behavior and data ownership are validated. |
| Splash/Auth/Biometric gate | app shell/auth routes | Keep | Startup, identity, guest mode, and app lock. | Must remain minimal, accessible, and fast. |

## Project Checklist

Information architecture:

- Primary bottom navigation contains only Home, Contacts, Events, Messages, and Analytics.
- Settings, AI Doctor, Backup/Restore, Activity History, and contextual tools are reachable in two taps or fewer from relevant primary screens.
- Each feature has one owner route listed in Screen Ownership Decisions.
- Duplicate controls are links or shortcuts, not independent implementations.
- Deep links and shortcuts route to current owner screens.

Layout and hierarchy:

- Each screen has one clear heading, one primary action area, and predictable secondary actions.
- Dense operational screens use lists, filters, tabs, and compact cards instead of marketing-style sections.
- Summary dashboards show only the highest-priority metrics and actions.
- Empty states explain what is missing and offer the next safe action.
- Loading states use progress indicators or skeletons where content shape is known.
- Error states show recovery actions and do not expose secrets or raw provider payloads.

Components:

- Shared UI uses `core/ui` tokens before introducing screen-local dimensions.
- Cards use radius 8 dp or less unless a component is a pill/chip.
- Icon buttons use recognizable icons with content descriptions when the icon is the only label.
- Buttons are reserved for commands; tabs, filters, chips, switches, checkboxes, sliders, and menus represent choices.
- No cards inside cards.
- No decorative gradient/orb backgrounds.

Accessibility:

- Touch targets are at least 48 dp where practical.
- Text has sufficient contrast against its container.
- User-facing icons have labels or content descriptions.
- State changes that need attention use polite live regions where appropriate.
- Large font scale review checks title truncation, button text wrapping, filter chips, cards, and bottom navigation.
- Hindi and English strings are checked for clipping and stale copy.

Performance:

- Large lists use lazy containers.
- Expensive work stays in ViewModels/use cases, not Composables.
- Empty/loading/error states render progressively.
- Screens do not block first paint on optional diagnostics or exports.

Validation:

- ViewModel tests cover behavior-preserving UI state.
- Screen interaction tests cover critical commands and disabled states.
- Screenshot or device validation covers primary workflows at small width and large font scale.
- `git diff --check`, focused tests, and relevant compile tasks pass before closing a UI slice.

## Open Redesign Work

- Completed slice 2026-06-27: AI Doctor keeps its current route and feature ownership, while the screen now uses shared spacing/radius/size/alpha tokens, keeps check actions attached to their owning row copy, and avoids nested cards in the recommended-fix surface.
- Completed slice 2026-06-28: AI Doctor compact-phone Roborazzi baselines now cover setup blockers, setup blockers at large font, the WhatsApp consent/setup-card region at large font, a healthy diagnostics state, and a refreshing state. The screenshots render deterministic `AutomationSetupUiState` fixtures through `AutomationSetupContent`, preserving ViewModel diagnostics, consent persistence, settings intents, and route callbacks.
- Completed slice 2026-06-27: Onboarding remains the setup bridge and does not duplicate AI Doctor diagnostics, while the hero, setup checklist rows, step badges, and primary/secondary actions now use shared spacing/radius/size tokens. The continue action still completes onboarding through the ViewModel, and the setup checklist action still opens AI Doctor/setup.
- Completed slice 2026-06-27: Auth remains the startup identity gate, while Google sign-in, debug-only guest bypass, signed-in routing, loading/error/legal surfaces, and debug visibility now have focused coverage. The screen uses shared spacing/radius/size/alpha tokens without changing launcher, ViewModel, or `BuildConfig.DEBUG` behavior.
- Completed slice 2026-06-27: Splash remains the startup routing gate, while brand, subtitle, fade, and progress indicator presentation now use shared spacing/size/alpha tokens with focused content coverage. Destination resolution timing and navigation callbacks are unchanged.
- Completed slice 2026-06-27: Splash compact-phone Roborazzi baselines now cover default and large-font startup rendering. The screenshot test freezes the pure `SplashContent` at a deterministic visible progress frame and does not change routing, animation timing, or destination resolution behavior.
- Completed slice 2026-06-27: Home keeps existing next-action, setup-progress, quick-action, planner, and upcoming-event behavior, while the screen now uses shared spacing/radius/size/alpha tokens, extracts stats/quick actions into presentation helpers, and reserves loading-panel space to avoid layout jump. Future IA cleanup must validate any quick-action removal before changing navigation affordances.
- Completed slice 2026-06-28: Home compact-phone Roborazzi baselines now cover the populated operational dashboard, large-font dashboard top, a lower large-font dashboard/action viewport, and the loading state. The screenshots render deterministic `HomeUiState` fixtures through `HomeContent` only, preserving ViewModel loading, navigation, and data behavior.
- Completed slice 2026-06-28: Home production color tokenization now routes the dashboard background, settings icon, loading spinner, next-action accents, setup-progress success/warning states, quick-action icons, planner accents, birthday date chips, and secondary supporting text through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. Navigation callbacks, next-action routing, setup-progress click behavior, quick actions, planner links, loading state, test tags, and ViewModel behavior are unchanged; `DesignSystemTokensTest` now guards `HomeScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-27: Contact List remains the primary Contacts owner, while search, filter chips, sort chips, refresh/sync-error surfaces, loading skeletons, empty state, row avatars, health dots, and quality labels now use shared spacing/radius/size/alpha/fraction tokens. Search, filter, sort, refresh, dismiss, and row navigation callbacks are unchanged.
- Completed slice 2026-06-28: Contact List Roborazzi baselines now cover populated compact-phone, populated typical-phone, sync-error compact-phone large-font, loading, and empty states. Visual review confirmed the search field, horizontal filter/sort rows, sync-error actions, skeleton rows, empty state, contact rows, and quality labels are nonblank and readable; search, filtering, sorting, refresh/dismiss, row navigation, and ViewModel behavior are unchanged.
- Completed slice 2026-06-28: Contact List production color tokenization now routes the screen background, search-field text and borders, search icons, avatar surfaces, secondary relationship text, and quality labels through `MaterialTheme.colorScheme`. Search, filtering, sorting, sync refresh/error dismissal, permission launch, row navigation, test tags, and ViewModel behavior are unchanged; `DesignSystemTokensTest` now guards `ContactListScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-27: Wish Preview keeps the existing single-draft review/editor behavior, while the screen now uses shared spacing/radius/size/alpha/fraction tokens across variants, editor, feedback chips, summary/why panels, metadata rows, and review actions.
- Completed slice 2026-06-28: Wish Preview compact-phone Roborazzi baselines now cover editor, editor large-font, blocked short-draft large-font, approved review-next, and loading states. The large-font blocked capture exposed clipped approval text in a fixed-height primary button; `RelatePrimaryButton` now keeps the same minimum touch target while allowing wrapped labels to grow, preserving callbacks and enabled/disabled behavior.
- Completed slice 2026-06-27: Backup/Restore remains a separate secondary data-safety route, while warning, passphrase, password-strength, export/import action, preview-confirmation, success, and error surfaces now use shared spacing/radius/size/alpha/elevation/fraction tokens. Export/import behavior and replace-restore confirmation are unchanged.
- Completed slice 2026-06-28: Backup/Restore compact-phone Roborazzi baselines now cover the initial passphrase/security-warning view, large-font export/restore actions, large-font restore preview confirmation, export progress, and a wrong-passphrase error status. The screenshots render deterministic `BackupRestoreUiState` fixtures through the existing pure `BackupRestoreContent`, preserving document pickers, passphrase validation, backup service calls, preview confirmation, status clearing, and back navigation.
- Completed slice 2026-06-28: Backup/Restore production color tokenization now routes the screen background, warning/error/success/status colors, passphrase strength colors, action-card surfaces, and export/import action icon states through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. Document picker launchers, passphrase gating, password-strength calculation, import-preview confirmation, replace restore, status dismissal, test tags, and callbacks are unchanged; `DesignSystemTokensTest` now guards `BackupRestoreScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-27: Contact Detail remains the contextual relationship workspace, while profile, essentials, personalization, automation, history, and preference-dialog surfaces now use shared spacing/radius/size tokens. Preference options wrap for longer labels without changing saved preference behavior.
- Completed slice 2026-06-28: Contact Detail compact-phone Roborazzi baselines now cover the profile/essentials top, personalization large-font state, automation/history large-font state, and loading state. The large-font automation capture exposed cramped three-column shortcuts that split "WhatsApp"; automation shortcuts now stack full-width while keeping the same actions and enabled-state rules.
- Completed slice 2026-06-27: Events remains the occasion workspace, while filters, manual-event choices, event cards, metadata chips, conflict actions, and snackbar surfaces now use shared spacing/radius/size/alpha tokens. Manual save validation, duplicate warnings, filtering, refresh, and conflict-resolution callbacks are unchanged.
- Completed slice 2026-06-28: Events compact-phone Roborazzi baselines now cover the populated schedule, conflict-resolution large-font state, empty state, and loading state. The screenshots render deterministic `EventsUiState` fixtures through `EventsContent`; `EventListItem.daysUntil(nowMs)` preserves the existing `daysUntil` behavior while allowing screenshot fixtures to use a fixed clock for stable event counters.
- Completed slice 2026-06-27: Messages remains the operational queue owner, while tabs, filters, bulk actions, pending/scheduled/failed/sent cards, readiness badges, queue actions, and recovery assistant surfaces now use shared spacing/radius/size/alpha tokens. Queue callbacks, selection behavior, and tab ownership are unchanged.
- Completed slice 2026-06-28: Messages compact-phone Roborazzi baselines now cover needs-review, needs-review large-font, failed recovery large-font, and loading states. The screenshots render deterministic `MessagesUiState` fixtures through `MessagesContent` only, preserving ViewModel refresh, filtering, selection, approval, retry, rejection, and navigation behavior.
- Completed slice 2026-06-27: Settings remains the global configuration route, while account identity, preferences, credentials, automation mode, quiet hours, channel blackouts, sync/data links, legacy notice, sign-out confirmation, snackbar, and dividers now use shared spacing/radius/size/alpha tokens. Credential saves, permission-gated sync, navigation links, and sign-out orchestration are unchanged.
- Completed slice 2026-06-28: Settings compact-phone Roborazzi baselines now cover the account/preferences overview, large-font AI/email/automation configuration, large-font data/sync tools with the legacy notice, and the sign-out confirmation dialog. `SettingsScreen` now keeps ViewModel, contacts permission, snackbar feedback, and sign-out orchestration in the runtime wrapper while `SettingsContent` renders deterministic state for visual tests; credential saves, sync permission gating, navigation links, and sign-out behavior are unchanged.
- Completed slice 2026-06-27: Analytics remains the reporting dashboard and export entry point, while stats, loading, report cards, monthly bar chart, relationship distribution, health, growth metrics, and neglected-contact rows now use shared spacing/radius/size tokens and theme semantic colors. Activity History stays linked instead of duplicating raw audit rows.
- Completed slice 2026-06-28: Analytics compact-phone Roborazzi baselines now cover the populated report top, large-font relationship health/growth/neglected reporting sections, empty report state, and loading state. The loading presentation now shows only the header and progress state until analytics data is ready instead of mixing the spinner with empty report sections; export/share callbacks, Activity History navigation, and ViewModel data aggregation are unchanged.
- Completed slice 2026-06-27: Activity History remains the secondary audit-trail owner, while search/filter spacing, loading/empty/error states, log cards, metadata rows, warning severity color, and contextual route actions now use shared spacing and semantic color tokens. Analytics and Settings may link here but do not duplicate raw audit rows.
- Completed slice 2026-06-28: Activity History compact-phone Roborazzi baselines now cover the populated audit trail, a large-font routed action card with active search/type/date/status filters, empty state, error state, and loading state. The screenshots render deterministic `ActivityHistoryUiState` fixtures through `ActivityHistoryContent`, preserving search, filters, back navigation, route callbacks, and ViewModel filtering behavior.
- Completed slice 2026-06-28: Activity History production color tokenization now routes loading, audit type icons, metadata/status labels, routed action buttons, and severity colors through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. Search, filters, back navigation, contextual route callbacks, status/severity labels, date formatting, test tags, and ViewModel filtering behavior are unchanged; `DesignSystemTokensTest` now guards `ActivityHistoryScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-27: Gift Advisor remains a contextual contact tool, while budget stats, AI suggestions, suggestion cards, history rows, delete affordance, feedback controls, record dialog, empty/error states, and loading rhythm now use shared spacing/radius/size/alpha/elevation tokens. Gift save/delete/suggestion behavior and validation are unchanged.
- Completed slice 2026-06-28: Gift Advisor compact-phone Roborazzi baselines now cover AI suggestions, large-font gift history, error/empty state, generating-progress state, and loading state. Visual review replaced cramped one-line budget stat labels with local two-line labels, moved gift history cost/actions below record metadata, and added bottom list padding so large-font history cards remain readable; gift save/delete/suggestion behavior and validation are unchanged.
- Completed slice 2026-06-27: Style Coach remains the writing-style training/history owner, while training input, manual/auto analyze controls, progress indicators, status message, learned-profile card, empty history, and history snapshots now use shared spacing/radius/size/alpha/elevation tokens. Manual parsing, analysis callbacks, busy states, and profile/history rendering are unchanged.
- Completed slice 2026-06-28: Style Coach compact-phone Roborazzi baselines now cover training entry, learned-profile large-font, history large-font, manual-progress, and auto-error/empty states. Visual review stacked history snapshot source/timestamp metadata and added bottom list padding so large-font history cards remain readable; manual parsing, analysis callbacks, busy states, and profile/history rendering are unchanged.
- Completed slice 2026-06-27: Memory Vault remains a contextual contact tool, while prompt chips, category chips, add-note form, note rows, pinned state, empty/error/loading states, and note metadata now use shared spacing/size/alpha/elevation tokens. Prompt selection, note validation, add, pin/unpin, delete, and back behavior are unchanged.
- Completed slice 2026-06-28: Memory Vault compact-phone Roborazzi baselines now cover the add-note form, populated notes, pinned-note large-font layout, error/empty state, and loading state. Visual review added bottom content padding to the journal list so note cards have better scroll breathing room; prompt selection, note validation, add, pin/unpin, delete, and back behavior are unchanged.
- Completed slice 2026-06-28: Memory Vault production color tokenization now routes loading/background, prompt/date supporting text, add-button and pinned-note accents, pinned/plain note surfaces, and disabled pin states through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. Prompt selection, note-length validation, blank-note validation, add, pin/unpin, delete, back navigation, test tags, and ViewModel behavior are unchanged; `DesignSystemTokensTest` now guards `MemoryVaultScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-27: Chat History remains a contextual read-only sent-message route, while loading, empty, error, back navigation, message cards, timestamp metadata, and list rhythm now use shared spacing tokens with focused coverage. It stays a merge candidate for a future Contact Detail history tab, but no behavior or data ownership changed.
- Completed slice 2026-06-28: Chat History compact-phone Roborazzi baselines now cover populated sent-message history, a long-message large-font state, empty state, error state, and loading state. The screenshots render deterministic `ChatHistoryUiState` fixtures through `ChatHistoryContent`, preserving the read-only message list, back callback, repository-backed ViewModel loading, and error behavior.
- Completed slice 2026-06-27: Shared cards, avatars, health indicators, and shimmer skeletons now consume spacing/size/alpha/fraction tokens instead of raw border widths, integer dp conversion, raw grays, or ad hoc health thresholds. Screen and shared-component token scans are clean for the reviewed patterns.
- Completed slice 2026-06-28: CI now verifies approved Roborazzi baselines on pushes and pull requests, uploads Roborazzi reports/outputs for mismatch review, and keeps baseline recording local-only so visual changes are paired with the UI/code change and documented in `PLAN.md` or this checklist.
- Completed slice 2026-06-28: First high-risk Hindi large-font Roborazzi baselines now cover Onboarding setup actions, Messages failed-send recovery, and Wish Preview blocked-review controls. The screenshots use Hindi resource qualifiers plus Devanagari fixture text for message/review content where the UI owns the copy; visual review found the localized labels, chips, actions, and warning copy readable without obvious clipping.
- Completed slice 2026-06-28: First typical-phone Roborazzi baselines now cover the Home populated dashboard, Messages needs-review queue, and Wish Preview editor at `w411dp-h891dp-xhdpi`. Visual review confirmed the wider captures are nonblank and framed correctly.
- Completed slice 2026-06-28: Messages queue pager pages now top-align populated list content and queue `LazyColumn`s fill the pager area, removing dead vertical space before the first card in compact, large-font, Hindi failed-recovery, and typical-phone captures. Tabs, filters, selection, approval, retry, rejection, edit, revoke, and ViewModel behavior are unchanged.
- Completed slice 2026-06-28: Additional high-risk Hindi large-font Roborazzi baselines now cover AI Doctor setup cards, Backup/Restore import-preview confirmation, and Settings data/sync tools. Visual review found the localized setup, restore-warning, sync, backup, and activity rows readable without obvious clipping; screenshot fixtures and anchors changed only for visual coverage, not runtime settings, backup, or diagnostics behavior.
- Completed slice 2026-06-28: Broader Hindi large-font Roborazzi baselines now cover Contact Detail personalization, Events conflict resolution, Analytics reporting sections, and Activity History routed action cards. Visual review kept Analytics fixture keys aligned with production data, switched the Events Hindi capture to Hindi date/month formatting, and localized Activity History severity/status row labels while preserving raw stored log values, search, filtering, and route callbacks.
- Completed slice 2026-06-28: Contextual Hindi large-font Roborazzi baselines now cover Chat History long sent messages, Memory Vault pinned notes, Gift Advisor suggestions, and Style Coach learned profiles. Visual review localized Chat History channel display labels, Style Coach known formality display labels, and Memory Vault/Style Coach note/history dates while preserving raw stored channel/formality/date data, repository behavior, and route callbacks.
- Completed slice 2026-06-28: Deeper Hindi large-font edge-state Roborazzi baselines now cover Activity History empty/error, Chat History empty/error, Memory Vault error-empty, Gift Advisor error-empty, and Style Coach auto-error-empty. Visual review confirmed the localized empty/error copy, filter chips, contextual headers, disabled actions, and persistent Gift Advisor bottom action remain readable without obvious clipping.
- Completed slice 2026-06-28: Broader typical-phone Roborazzi baselines now cover AI Doctor blockers, Backup/Restore actions, Settings overview, Analytics populated reporting, and Activity History populated audit trail at `w411dp-h891dp-xhdpi`. Visual review confirmed the wider captures are nonblank and readable, with horizontally scrollable filter rows continuing to behave as intended.
- Completed slice 2026-06-28: Remaining primary/contextual typical-phone Roborazzi baselines now cover Contact Detail profile, Events schedule, Chat History populated sent messages, Memory Vault notes, Gift Advisor suggestions, and Style Coach training at `w411dp-h891dp-xhdpi`. Visual review found the Gift Advisor overlay record FAB could cover history content, so the same record action now uses a scaffold-reserved bottom action bar with the existing callback/test tag; contact detail, event, chat, memory, gift, and style coach behavior is unchanged.
- Completed slice 2026-06-28: Startup/auth/onboarding typical-phone Roborazzi baselines now cover Splash default, Auth default, Onboarding default, and Onboarding setup actions at `w411dp-h891dp-xhdpi`. Visual review confirmed the startup logo/progress, sign-in/dev-bypass actions, setup checklist, and setup action buttons are nonblank, readable, and framed without clipping; routing, sign-in callbacks, dev-bypass callback, onboarding continue, and setup navigation behavior are unchanged.
- Completed slice 2026-06-28: Deeper primary/reporting typical-phone Roborazzi state baselines now cover Home loading, Messages loading, Wish Preview loading, Events empty/loading, Analytics empty/loading, and Activity History empty/error/loading at `w411dp-h891dp-xhdpi`. Visual review confirmed the ten state captures are nonblank, readable, and framed correctly; existing dashboard shell, action callbacks, filters, exports, route callbacks, and loading/empty/error behavior are unchanged.
- Completed slice 2026-06-28: Deeper contextual typical-phone Roborazzi state baselines now cover Chat History empty/error/loading, Memory Vault error-empty/loading, Gift Advisor error-empty/generating/loading, and Style Coach manual-progress/auto-error-empty at `w411dp-h891dp-xhdpi`. Visual review confirmed the ten state captures are nonblank, readable, and preserve contextual route headers, loading/empty/error placement, and the Gift Advisor reserved bottom record action.
- Completed slice 2026-06-28: Secondary/configuration typical-phone Roborazzi baselines now cover Settings AI configuration, Settings data tools, Settings sign-out dialog, Backup/Restore passphrase, Backup/Restore import preview, Backup/Restore exporting, and Backup/Restore error at `w411dp-h891dp-xhdpi`. Visual review confirmed the seven captures are nonblank, readable, and preserve settings save/sync/navigation/sign-out surfaces plus backup passphrase/export/import/status behavior.
- Completed slice 2026-06-28: Secondary/scrolled typical-phone Roborazzi baselines now cover Messages failed recovery, Wish Preview approved review-next, Contact Detail automation/history, AI Doctor healthy/refreshing, Events conflict resolution, Gift Advisor history, and Style Coach profile/history at `w411dp-h891dp-xhdpi`. Visual review confirmed the nine captures are nonblank/readable, secondary sections are intentionally anchored, horizontal filter rows remain scrollable, and the Gift Advisor persistent bottom action does not overlap history content.
- Completed slice 2026-06-28: Theme-mode decision is now explicit: RelateAI ships the validated dark theme only for this redesign, guarded by `RelateThemeContractTest`. Light/dynamic color is deferred until dark-specific exported color use is replaced by semantic/theme-backed tokens and the new mode has screenshot and contrast evidence.
- Completed slice 2026-06-28: Messages reject confirmation dialog baselines now cover compact and typical phone. Visual review confirmed the title, body copy, cancel action, and destructive reject action fit without clipping; Contact Preferences, Events manual entry, and Gift Advisor add-dialog remained excluded at this point because dense text-field platform dialogs did not idle reliably under the JVM screenshot harness.
- Completed slice 2026-06-28: Messages reject confirmation dialog now also has a Hindi large-font compact-phone baseline. Visual review confirmed the localized title, body copy, cancel action, and destructive reject action remain readable and fit without clipping.
- Completed slice 2026-06-28: Messages needs-review queue now also has a Hindi large-font compact-phone baseline. Visual review confirmed the localized title, tabs, search, filters, readiness badge, message body, and action buttons remain readable; the tab row remains horizontally scrollable at large font and the primary approve label wraps inside its button without overlapping.
- Completed slice 2026-06-28: Gift Advisor add-gift form coverage now uses the same extracted `AddGiftDialogBody` that production renders inside the existing dialog, captured through deterministic top and scrolled-bottom compact-phone large-font fixtures. Visual review confirmed the title, required fields, cost field, feedback controls, notes field, character counter, Cancel action, and Save record action remain readable without overlap; the platform `AlertDialog` window itself remains excluded from Roborazzi because it still does not idle reliably with dense text fields.
- Completed slice 2026-06-28: Contact Preferences form coverage now uses the same extracted `ContactPreferencesDialogBody` that production renders inside the existing dialog, captured through deterministic top and scrolled-bottom compact-phone large-font fixtures. Visual review confirmed the title, nickname/relationship fields, language/channel/formality choices, lower preference fields, notes, skip-auto-wish switch, Cancel action, and Save action remain readable without overlap; preference save parsing, typed channel/automation choices, fallback handling, and dialog callbacks are unchanged.
- Completed slice 2026-06-28: Events manual-entry form coverage now uses the same extracted `ManualEventDialogBody` that production renders inside the existing dialog, captured through deterministic existing-contact, new-contact, and scrolled-bottom warning compact-phone large-font fixtures. Visual review confirmed the title, contact-mode switch, contact/name fields, event-type choices, label, date fields, duplicate/date-conflict warning, Cancel action, Save action, and Save anyway action remain readable without overlap; manual-event validation, duplicate-warning allowance, conflict handling, and ViewModel save behavior are unchanged.
- Completed slice 2026-06-28: Shared status color tokenization now starts with `MaterialTheme.relateSemanticColors`, `AdaptiveFeedbackBanner`, `StatusIndicator`, and the shared `SyncErrorCard`. `SyncErrorCard` no longer owns raw amber color or local dp values; Home and Contact List retry/dismiss behavior is unchanged. The new Home sync-error compact-phone large-font baseline confirms the warning title, message, Dismiss action, Retry action, and following dashboard cards remain readable without overlap.
- Completed slice 2026-06-28: Shared component color tokenization now routes `RelateScreen`, `RelateTopBar`, `RelateGlassCard`, `RelatePrimaryButton`, `RelateStatusBanner`, `RelateAvatar`, `relateTextFieldColors`, `StatCard`, `HealthIndicatorDot`, `HealthBar`, `EmptyState`, `FilterChip`, and `ShimmerItem` through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. The shared primitives no longer import dark-specific color exports directly; card container/outline roles remain visually equivalent through semantic theme values.
- Completed slice 2026-06-28: Events production color tokenization now routes screen background, add-event/action accents, loading indicator, event icon surfaces, metadata chips, secondary labels, and duplicate/date-conflict warning accents through `MaterialTheme.colorScheme` or `MaterialTheme.relateSemanticColors`. Event filtering, manual-entry validation, duplicate-warning override, conflict actions, snackbar feedback, test tags, and ViewModel callbacks are unchanged; `DesignSystemTokensTest` now guards `EventsScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Chat History production color tokenization now routes the loading spinner and sent-message metadata labels through `MaterialTheme.colorScheme`. Contextual navigation, read-only message history, localized channel display labels, timestamps, loading/empty/error states, message test tags, and ViewModel behavior are unchanged; `DesignSystemTokensTest` now guards `ChatHistoryScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Splash production color tokenization now routes the startup background, app-title accent, subtitle color, and progress indicator through `MaterialTheme.colorScheme`. Fade timing, progress test tag, two-second destination delay, `SplashViewModel.resolveDestination()` routing, and navigation callbacks are unchanged; `DesignSystemTokensTest` now guards `SplashScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Auth production color tokenization now routes the sign-in background, app-title accent, subtitle/legal/error copy, loading indicator, Google sign-in button, and debug bypass button colors through `MaterialTheme.colorScheme`. Google sign-in launcher flow, debug bypass visibility, loading/error branches, auth-complete callback, test tags, spacing, sizing, and typography are unchanged; `DesignSystemTokensTest` now guards `AuthScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Onboarding production color tokenization now routes the setup background, hero/setup icons, subtitle and description text, continue action, setup checklist button text, and step-number surfaces through `MaterialTheme.colorScheme`. Setup checklist content, scroll behavior, continue/setup callbacks, test tags, spacing, sizing, and typography are unchanged; `DesignSystemTokensTest` now guards `OnboardingScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Style Coach production color tokenization now routes the screen background through `MaterialTheme.colorScheme` and success status feedback through `MaterialTheme.relateSemanticColors`. Manual training, auto analysis, status/error display, profile/history rendering, JSON parsing, callbacks, progress tags, and screenshot fixtures are unchanged; `DesignSystemTokensTest` now guards `StyleCoachScreen.kt` against direct dark-specific color exports.
- Completed slice 2026-06-28: Backup/Restore production color tokenization now routes the screen background, security warning surface, passphrase strength labels/progress, status success/error text, dismiss action, action-card container, and enabled/disabled action icons through theme-backed roles. Export/import launchers, passphrase validation, restore preview confirmation, replace behavior, and status clearing remain unchanged.
- Continue expanding Roborazzi screenshot coverage beyond the Splash/Auth/Onboarding/Home/Messages/Wish Preview/Contact Detail/Events/AI Doctor/Backup/Restore/Settings/Analytics/Activity History/Chat History/Memory Vault/Gift Advisor/Style Coach compact-phone pilots, including remaining scrolled edge states, any newly introduced dialog fixtures, and high-risk Hindi variants.
- Run a final screen-local token scan after each UI slice and document any intentional exceptions.
- Split oversized screens after behavior is covered by tests: Messages, Contact Detail, Events, Settings, Gift Advisor, Wish Preview, Home, and AI Doctor.
- Re-evaluate Analytics as a primary bottom-nav item after usage review.
